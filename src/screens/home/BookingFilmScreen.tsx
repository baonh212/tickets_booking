import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const BookingFilmScreen = () => {
  return (
    <View style={styles.container}>
      <Text>BookingFilmScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
